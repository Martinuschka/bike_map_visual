# bike_map_visual

this python script takes measurements from my BikeMap-App (Android) and creates a plot using OSM (Folium)

used as automated visualisation of data (Cron)

## pyinstaller

```bash
sudo pyinstaller --onefile --paths ./venv/lib/python3.10/site-packages main.py
```
